export declare const MAX_HEADER_LEN_B64 = 248;
export declare const MAX_PADDED_UNSIGNED_JWT_LEN: number;
export declare function lengthChecks(jwt: string): void;
export declare function jwtToAddress(jwt: string, userSalt: string | bigint): string;
export interface ComputeZkLoginAddressOptions {
    claimName: string;
    claimValue: string;
    userSalt: string | bigint;
    iss: string;
    aud: string;
}
export declare function computeZkLoginAddress({ claimName, claimValue, iss, aud, userSalt, }: ComputeZkLoginAddressOptions): string;
